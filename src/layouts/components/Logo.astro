---
import { LogoFullIcon } from '@components/icons';

interface Props {
  lang?: string;
}

const { lang = 'es' } = Astro.props;
---

<a
  href={`/${lang}`}
  class="flex items-center gap-2 hover:opacity-80 transition-opacity cursor-pointer"
>
  <LogoFullIcon className="h-10 w-auto sm:h-12 md:h-16 lg:h-20 xl:h-24" />
</a>
